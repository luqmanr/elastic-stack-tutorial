log_format main '$remote_addr - $remote_user $http_x_clientid [$time_local] "$host" "$request" '
            '$status $body_bytes_sent "$http_referer" '
            '$request_body '
            '"$http_user_agent" $request_time';